<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileexercice">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileExercice">
        <activity
            android:name=".ActivityPrincipal"
            android:label="@string/title_activity_inicio"
            android:theme="@style/Theme.MobileExercice.NoActionBar"></activity>
        <activity
            android:name=".TelaCadastro2"
            android:label="@string/title_activity_tela_cadastro2"
            android:theme="@style/Theme.MobileExercice.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MobileExercice.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>